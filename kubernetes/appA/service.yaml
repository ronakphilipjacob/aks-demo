apiVersion: v1
kind: Service
metadata:
  name: appa-service
  namespace: app-a
  labels:
    app: appa
spec:
  selector:
    app: appa
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer