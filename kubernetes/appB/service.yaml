apiVersion: v1
kind: Service
metadata:
  name: appb-service
  namespace: app-b
  labels:
    app: appb
spec:
  selector:
    app: appb
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer